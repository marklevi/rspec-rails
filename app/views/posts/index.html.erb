<% if @posts.any? %>
  <% @posts.each do |post| %>
    <h4><%= link_to "#{post.title}", post %></h4>
    <%= link_to "Edit", edit_post_path(post)%>
    <%= simple_format post.content %>


    <hr>
  <% end %>
<% end %>

